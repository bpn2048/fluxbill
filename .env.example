# Root .env is not required for docker-compose itself, but copies of
# backend/.env.example -> backend/.env and frontend/.env.example -> frontend/.env.local
# are. See docs/LOCAL_DEV.md.

# Minimum to make the AI assistant work locally:
# OPENROUTER_API_KEY=...
